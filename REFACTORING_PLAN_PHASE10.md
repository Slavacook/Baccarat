# План рефакторинга ФАЗА 10: CameraManager

**Цель**: Уменьшить `CameraManager` с 892 строк до ~400-500 строк  
**Текущий размер**: 892 строки  
**Целевой размер**: ~400-500 строк  
**Нужно убрать**: ~400 строк

---

## 📊 Анализ текущего состояния

### Размер и структура
- **Строк кода**: 892
- **Методов**: 30
- **Сигналов**: 2 (`zoom_started`, `zoom_completed`)
- **Логических групп**: 9

### Уже проанализировано ✅
- Создан `CAMERAMANAGER_ANALYSIS.md` с детальным анализом
- Выявлены 4 кандидата для извлечения

---

## 🎯 Кандидаты для извлечения

### 1. CameraInterpolationHandler (~200 строк) 🔴 Высокий приоритет

**Методы для извлечения:**
- `_process_interpolation(delta)` - обработка экспоненциального сглаживания
- `_animate_to(target_pos, target_zoom, target_rotation, zoom_type, is_navigation)` - анимация камеры (основная логика)

**Зависимости:**
- `_config: CameraConfig`
- `_camera: Camera2D`
- `_scene: Node` (для создания Tween)
- `_target_position`, `_target_zoom`, `_target_rotation` (целевые значения)
- `_is_interpolating`, `_current_zoom_type`, `_is_navigation_mode` (состояние)

**Оценка**: ~200 строк, средний риск ⚠️

**Примечание**: Самая сложная часть CameraManager. Требует передачи состояния.

---

### 2. CameraAreaNavigator (~130 строк) 🟡 Средний приоритет

**Методы для извлечения:**
- `_zoom_next_area()` - переключиться на следующую область
- `_zoom_prev_area()` - переключиться на предыдущую область
- `_zoom_up()` - вертикальная навигация вверх
- `_zoom_down()` - вертикальная навигация вниз
- `_get_target_area_by_direction(direction)` - определить целевую область по направлению
- `_get_target_area_by_direction_from(area, direction)` - определить целевую область из указанной
- `predict_target_area(zoom_type)` - предсказать целевую область

**Зависимости:**
- `_config: CameraConfig` (для получения позиций областей)
- `current_area: int` (текущая область)
- `last_zoom_type: String` (последний тип зума)

**Оценка**: ~130 строк, низкий риск ✅

---

### 3. CameraZoomHandler (~30 строк) 🟢 Низкий приоритет

**Методы для извлечения:**
- `_zoom_in(is_navigation)` - зум на область карт
- `_zoom_out(is_navigation)` - возврат к общему плану
- `_zoom_mode2_right(is_navigation)` - зум на режим 2 (справа)
- `_zoom_mode2_left(is_navigation)` - зум на режим 2 (слева)
- `_zoom_area(area_index, is_navigation)` - зум на указанную область

**Зависимости:**
- `_config: CameraConfig` (для получения позиций)
- `_animate_to()` (для выполнения анимации)

**Оценка**: ~30 строк, низкий риск ✅

**Примечание**: Эти методы в основном вызывают `_animate_to()`, поэтому извлечение может быть простым.

---

### 4. CameraAnimationHelper (~50 строк) 🟢 Низкий приоритет

**Методы для извлечения:**
- `_get_transition_type(type_name)` - преобразование типа анимации
- `_get_ease_type(type_name)` - преобразование типа плавности
- `_get_zoom_name(zoom_type)` - получить человекочитаемое название зума

**Зависимости:**
- Нет (статические утилиты)

**Оценка**: ~50 строк, низкий риск ✅

---

## 📊 Ожидаемые результаты

### После извлечения всех классов:
- **CameraManager**: ~450 строк (было 892) ✅ **-50%**
- **CameraInterpolationHandler**: ~200 строк (новый)
- **CameraAreaNavigator**: ~130 строк (новый)
- **CameraZoomHandler**: ~30 строк (новый)
- **CameraAnimationHelper**: ~50 строк (новый)

**Всего извлечено**: ~410 строк

---

## 🎯 План выполнения

### Шаг 1: Извлечь CameraAnimationHelper
- Создать класс `CameraAnimationHelper`
- Извлечь утилиты анимации
- Обновить `CameraManager` для использования нового класса

**Приоритет**: Низкий, но простой для начала

---

### Шаг 2: Извлечь CameraZoomHandler
- Создать класс `CameraZoomHandler`
- Извлечь методы зума
- Обновить `CameraManager` для использования нового класса

**Приоритет**: Низкий, логически изолированная группа

---

### Шаг 3: Извлечь CameraAreaNavigator
- Создать класс `CameraAreaNavigator`
- Извлечь методы навигации по областям
- Обновить `CameraManager` для использования нового класса

**Приоритет**: Средний, более сложная логика

---

### Шаг 4: Извлечь CameraInterpolationHandler
- Создать класс `CameraInterpolationHandler`
- Извлечь логику интерполяции и анимации
- Передать состояние через параметры или свойства
- Обновить `CameraManager` для использования нового класса

**Приоритет**: Высокий, самая сложная часть

---

## ⚠️ Важные замечания

1. **Состояние камеры** - много переменных состояния, нужно аккуратно передавать
2. **Зависимости от CameraConfig** - все классы будут нуждаться в доступе к конфигурации
3. **Tween создание** - `_animate_to()` создает Tween, нужно передать `_scene`
4. **Сигналы** - `zoom_started` и `zoom_completed` должны остаться в CameraManager

---

## ✅ Критерии успеха

- [ ] CameraManager < 500 строк
- [ ] Все тесты проходят
- [ ] Игра работает как раньше
- [ ] Нет ошибок компиляции
- [ ] Сохранена обратная совместимость
- [ ] Сигналы работают корректно

---

## 📝 Примечания

- Начать с простых классов (CameraAnimationHelper, CameraZoomHandler)
- Затем перейти к более сложным (CameraAreaNavigator, CameraInterpolationHandler)
- Тестировать после каждого шага
- Сохранять состояние камеры в CameraManager, передавать через параметры

