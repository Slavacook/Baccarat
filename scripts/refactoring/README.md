# 🔧 РЕФАКТОРИНГ ПРОЕКТА BACCARAT

Эта директория содержит документацию и временные файлы для пошагового рефакторинга проекта.

---

## 📁 Структура файлов

```
scripts/refactoring/
├── README.md                    # Этот файл (обзор)
├── PHASE_0_BASELINE.md          # Baseline метрики (до рефакторинга)
├── TESTING_CHECKLIST.md         # Чеклист тестирования
├── PHASE_N_PLAN.md              # План для каждой фазы (создаётся по мере выполнения)
└── PHASE_N_TEST_RESULTS.md      # Результаты тестирования каждой фазы
```

---

## 🗺️ Общий план рефакторинга

### Текущая фаза: **ФАЗА 0 - Подготовка** ✅

### Фазы (порядок выполнения):

| Фаза | Название | Время | Риск | Статус |
|------|----------|-------|------|--------|
| **0** | Подготовка (baseline) | 1-2ч | 🟢 Низкий | ✅ Завершена |
| **1** | EventBus миграция | 2-3ч | 🟢 Низкий | ⏳ Готова к началу |
| **2** | UIManager декомпозиция | 4-6ч | 🟡 Средний | 📋 Запланирована |
| **3** | GamePhaseManager декомпозиция | 5-7ч | 🟡 Средний | 📋 Запланирована |
| **4** | Dependency Injection | 6-8ч | 🔴 Высокий | ⚠️ Опционально |
| **5** | ServiceFactory + Locator | 4-5ч | 🟡 Средний | ⚠️ Опционально |

**Общее время (минимум)**: 12-18 часов (фазы 0-3)
**Общее время (полное)**: 22-31 час (фазы 0-5)

---

## 🎯 Цели рефакторинга

### Основные проблемы (до рефакторинга):

1. **God Objects** - слишком большие классы:
   - `GameController.gd` (1,310 строк)
   - `GamePhaseManager.gd` (604 строки)
   - `UIManager.gd` (319 строк)

2. **Прямые вызовы** - обход EventBus:
   - `game_controller.survival_ui.lose_life()`
   - `game_controller.payout_queue_manager = null`
   - ~10 мест в коде

3. **Низкая тестируемость** - отсутствие Dependency Injection

### После рефакторинга:

✅ Все классы <300 строк (кроме главного контроллера)
✅ Все коммуникации через EventBus
✅ Легко писать юнит-тесты (DI)
✅ Модульная архитектура (легко расширять)

---

## 📋 Краткое описание фаз

### Фаза 0: Подготовка (✅ завершена)

**Цель**: Зафиксировать текущее состояние для сравнения

**Задачи**:
- ✅ Git ветка `refactoring/phase-0-prep`
- ✅ Baseline метрики (7,501 строк, 42 файла)
- ✅ Snapshot функционала
- ✅ Чеклист тестирования

**Результат**: Документация в `PHASE_0_BASELINE.md` и `TESTING_CHECKLIST.md`

---

### Фаза 1: EventBus миграция (⏳ готова к началу)

**Цель**: Убрать все прямые вызовы между менеджерами

**Проблема**:
```gdscript
# ❌ Сейчас - прямой вызов
game_controller.survival_ui.lose_life()

# ✅ После - через EventBus
EventBus.life_lost.emit(remaining_lives - 1)
```

**Изменения**:
- Добавить события в `EventBus.gd`
- Удалить `game_controller` ссылку из `GamePhaseManager`
- Все взаимодействия через события

**Файлы**:
- `scripts/autoload/EventBus.gd` (+5 строк)
- `scripts/GamePhaseManager.gd` (-3 строки)
- `scripts/GameController.gd` (-1 строка)

**Риск**: 🟢 Низкий

---

### Фаза 2: UIManager декомпозиция (📋 запланирована)

**Цель**: Разбить God Object на специализированные менеджеры

**Было**:
```
UIManager.gd (319 строк, 30+ полей)
└── Всё в одном классе
```

**Станет**:
```
scripts/ui/
├── UIManager.gd (80 строк - фасад)
├── CardUIManager.gd (150 строк - карты + анимации)
├── ToggleUIManager.gd (80 строк - toggles)
├── MarkerUIManager.gd (60 строк - маркеры)
└── ButtonUIManager.gd (60 строк - кнопки)
```

**Файлы**: 5 новых, 2 изменённых
**Риск**: 🟡 Средний

---

### Фаза 3: GamePhaseManager декомпозиция (📋 запланирована)

**Цель**: Разделить обязанности фазового менеджера

**Было**:
```
GamePhaseManager.gd (604 строки)
└── Управление руками + раздача + валидация + UI
```

**Станет**:
```
scripts/game_logic/
├── HandManager.gd (120 строк - руки)
├── CardDealingCoordinator.gd (200 строк - раздача)
├── ActionValidator.gd (150 строк - валидация)
└── GamePhaseManager.gd (150 строк - координатор)
```

**Файлы**: 3 новых, 2 изменённых
**Риск**: 🟡 Средний

---

### Фаза 4-5: Dependency Injection (⚠️ опционально)

**Цель**: Полная тестируемость через DI контейнер

**Только если**:
- Планируется большое расширение проекта
- Нужны интеграционные тесты
- Есть время (10-13 часов)

**Риск**: 🔴 Высокий (затрагивает всю архитектуру)

---

## 🧪 Тестирование

### После каждой фазы ОБЯЗАТЕЛЬНО:

1. **GUT тесты** - все должны пройти ✅
   ```bash
   # В Godot Editor: нижняя панель → GUT → Run All
   ```

2. **Ручное тестирование** - см. `TESTING_CHECKLIST.md`
   - 12 обязательных сценариев
   - Проверка консоли на ошибки
   - Проверка производительности

3. **Checkpoint commit**:
   ```bash
   git add .
   git commit -m "REFACTOR: Фаза N завершена"
   git tag refactor-phase-N
   ```

### Если тесты не проходят:

1. ❌ **НЕ продолжайте** рефакторинг!
2. 🔍 Найдите проблему (git diff)
3. 🔧 Исправьте или откатите:
   ```bash
   git reset --hard HEAD  # Откат всех изменений
   git checkout имя_файла.gd  # Откат одного файла
   ```

---

## 🚨 План отката

```bash
# Откат текущей фазы
git checkout main
git branch -D refactoring/phase-N

# Откат к предыдущему checkpoint
git checkout refactoring/checkpoint-1

# Полный откат всего рефакторинга
git reset --hard origin/main
```

---

## 📊 Отслеживание прогресса

### Ключевые метрики:

| Метрика | До | Цель | Текущее |
|---------|-----|------|---------|
| GameController (строки) | 1,310 | <500 | 1,310 |
| UIManager (строки) | 319 | <150 | 319 |
| GamePhaseManager (строки) | 604 | <200 | 604 |
| Прямых вызовов game_controller | ~10 | 0 | ~10 |
| God Objects (>500 строк) | 3 | 0 | 3 |

Обновляется после каждой фазы в `PHASE_N_TEST_RESULTS.md`.

---

## 📝 Git workflow

### Ветки:

```
main (production)
│
├── refactoring/phase-0-prep (✅ текущая)
├── refactoring/phase-1-eventbus (⏳ следующая)
├── refactoring/phase-2-ui-decomposition
├── refactoring/phase-3-phase-manager-decomposition
└── refactoring/phase-4-dependency-injection
```

### Коммиты:

```bash
# После завершения фазы
git add scripts/refactoring/PHASE_N_*
git commit -m "docs: Фаза N - документация"

git add scripts/
git commit -m "REFACTOR(phase-N): краткое описание изменений"

git tag refactor-phase-N
```

### Слияние с main:

```bash
# После прохождения всех тестов
git checkout main
git merge --no-ff refactoring/phase-N
git push origin main
git push --tags
```

---

## 🎓 Принципы рефакторинга

### ✅ DO (делать):

1. **Маленькие шаги** - по одной фазе за раз
2. **Частые коммиты** - каждые 30-60 минут
3. **Тестирование** - после каждого изменения
4. **Документирование** - что изменили и почему
5. **Сохранение функционала** - работает как раньше

### ❌ DON'T (не делать):

1. **Большие изменения** - несколько фаз одновременно
2. **Пропуск тестов** - "потом протестирую"
3. **Изменение поведения** - "заодно исправлю баг"
4. **Удаление комментариев** - они помогают понять логику
5. **Оптимизация** - "заодно ускорю код"

---

## 📖 Дополнительные ресурсы

- **CLAUDE.md** - документация для Claude Code
- **project.godot** - конфигурация autoload синглтонов
- **tests/** - существующие GUT тесты

---

## 👤 Контакты

Вопросы по рефакторингу:
- Проект: Baccarat 5.9
- Разработчик: @vaaceslav
- Дата начала: 2025-12-04

---

Создано: 2025-12-04
Автор: Claude Code
Версия: 1.0
